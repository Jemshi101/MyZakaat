package com.quartzbit.myzakaat.firebase.firebaseParsers;

/**
 * Created by Jemsheer K D on 23 November, 2017.
 * Package com.quartzbit.myzakaat.firebase.firebaseParsers
 * Project Dearest
 */

public class RequestParser extends BasicParser {

   /* public RequestBean getRequest(DataSnapshot dataSnapshot) {
        if (dataSnapshot.exists()) {
            RequestBean requestBean = new RequestBean();
            requestBean.setId(getString(dataSnapshot, "id"));
            requestBean.setType(getInt(dataSnapshot, "type"));
            requestBean.setRequestTime(getLong(dataSnapshot, "requestTime"));
            requestBean.setMessage(getString(dataSnapshot, "message"));
            requestBean.setUserID(getString(dataSnapshot, "userID"));
            requestBean.setSubAdminID(getString(dataSnapshot, "subAdminID"));
            requestBean.setSubAdminName(getString(dataSnapshot, "subAdminName"));
            requestBean.setReferrerName(getString(dataSnapshot, "referrerName"));
            requestBean.setReferrerID(getString(dataSnapshot, "referrerID"));
            requestBean.setRelation(getString(dataSnapshot, "relation"));
            requestBean.setSelectedID(getString(dataSnapshot, "selectedID"));
            requestBean.setProfileID(getString(dataSnapshot, "profileID"));
            requestBean.setName(getString(dataSnapshot, "name"));
            requestBean.setProfilePhoto(getPhotoPath(dataSnapshot, "profilePhoto"));
            requestBean.setProfilePhotoThumb(getPhotoPath(dataSnapshot, "profilePhotoThumb"));

            return requestBean;
        } else {
            return null;
        }
    }*/
}
